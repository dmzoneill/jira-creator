Core
  _ mask private methods with _ prefix
  - type hardening
  - logging
  - Type Exceptions
    - raise exceptions
  - Docstrings
  - move status enums etc (31 Refinemnt) to config file
    - validation-issue factory pattern
      - new, refinement, backlog, in progress, closed, review
  - grep for os.environ.get

Add
  - list-blocked
  - list-sprints
  - set-summary
  - In Progress or blocked status
  - add-flag
  - remove-flag
  - clone / copy issue

Modify 
  - set-satus
    - move to stop of sprint if (refinement)

  - list-issues
    - fix default output
    - epic column 
    - assignee column 
    - reporter column
    - blocked column
    - acceptance criteria column
    - when --assignee or --reporter is empty or "me" get the current user 
    - add --columns option in list_issues
    - add --sort option in list_issues
    - add --blocked
    - Story points
      - round 3.0 to 3
      - None = 0

  - lint-issues
    - show description

  - set-acceptance-criteria 
    - --ai-from-description

  - view-issue
    - validation-issue factory pattern
    Custom output for
    - epic
    - story
    - bug
    - task
    - spike

  - list-issues
    - fix default output
    - epic column 
    - assignee column 
    - reporter column
    - blocked column
    - acceptance criteria column
    - when --assignee or --reporter is empty or "me" get the current user 
    - add --columns option in list_issues
    - add --sort option in list_issues
    - add --blocked

  - add-sprint
    - assign default to current user
    - or -- assign to specifc user

  - edit-issue --acceptance-criteria
    - Update acepance criteria from descirption (--ai) or user input
    - create linked issue
    - move to stop of sprint if (refinement)

  - create-issue
    - move to stop of sprint if (refinement)


Bug
  - add comment is loading story prompt, not comment prompt